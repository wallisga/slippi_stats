[tool:pytest]
# pytest configuration for Slippi Server
testpaths = tests
python_paths = .
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning